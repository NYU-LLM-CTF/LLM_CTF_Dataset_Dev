version: '3'
services:
  server:
    image: llmctf/2016q-pwn-hungman
    ports:
      - 9000:9000
    networks:
      ctfnet:
        aliases:
          - pwn.chal.csaw.io

networks:
  ctfnet:
    external: true