version: '3'
services:
  server:
    image: llmctf/2015q-pwn-autobots
    ports:
      - 8888:8888
    networks:
      ctfnet:
        aliases:
          - pwn.chal.csaw.io

networks:
  ctfnet:
    external: true